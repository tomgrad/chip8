{
    "tasks": [
        {
            "type": "shell",
            "label": "make Debug",
            "command": "/usr/bin/make",
            "options": {
                "cwd": "${workspaceFolder}/Debug"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}